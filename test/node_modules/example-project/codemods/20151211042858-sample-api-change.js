
module.exports = {
  moduleApiChange: true,
  transform: ({file, root, api, options}) => {
    const j = api.jscodeshift;

    const printOptions = options.printOptions || {};

    const didTransform = false;

    return {
      didTransform,
      root,
      printOptions
    };
  }
}
